import 'package:flutter/material.dart';

class AppColors {
  // Kite-style clean, minimal, blue-based
  static const background = Color(0xFFF8F9FB);
  static const cardBackground = Colors.white;

  static const primary = Color(0xFF387ED1);
  static const primaryDark = Color(0xFF2A5F9E);
  static const primaryLight = Color(0xFFEAF2FB);

  static const textPrimary = Color(0xFF191919);
  static const textSecondary = Color(0xFF6C7080);
  static const textMuted = Color(0xFF9DA3AF);

  static const border = Color(0xFFE6E9ED);
  static const success = Color(0xFF00A870);
  static const danger = Color(0xFFEB5B3C);

  static const navBackground = Colors.white;
}
